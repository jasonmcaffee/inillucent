# The compiler this repository is built and graded with.
#
# **Pinned, and the pin is what makes a result repeatable.** Every number this
# project publishes - the performance ratios in `docs/performance.md`, the
# 416-case feature comparison, the peak resident set the gate grades against -
# was produced by one compiler, and a machine that builds with a different one
# is measuring something else. Before task-1894 there was no pin at all, so
# `cargo clippy -D warnings` passed or failed depending on which Rust the person
# running it happened to have.
#
# It also fixes the lint set. A new compiler brings new lints, and the strict
# lint run is a required check: without a pin, a release that passed CI on
# Tuesday fails it on Wednesday because rustup updated, and the failure is about
# nothing the repository changed.
#
# ## Raising it
#
# Bump `channel`, run `cargo clippy --workspace --all-targets --all-features --
# -D warnings` and fix or narrowly allow what the new compiler found, then
# re-run the perf gate - a compiler change moves the numbers, and the history in
# `tests/performance-history.tsv` has to record which compiler produced each row
# or the series compares two different things.
[toolchain]
channel = "1.95.0"
components = ["rustfmt", "clippy"]
# The targets release packaging builds. Naming them here means `rustup` installs
# what a release needs on a fresh machine rather than failing part way through
# `packaging/release.sh`.
#
# `aarch64-unknown-linux-gnu` was missing until task-1934, and its absence is why
# no release before 0.1.2 carried an ARM Linux archive: `packaging/release-all.ps1`
# asks cargo-zigbuild for it by default and cargo answered "can't find crate for
# `core`". zig supplies the linker and the glibc floor; the standard library for
# the target still has to be installed, and this is what installs it.
#
# The two Apple triples were missing for the same reason and were not noticed at
# the same time (task-1951). `release-all.ps1 -Targets all` builds them, and on a
# machine with the pinned toolchain and nothing else cargo answers
#
#     error[E0463]: can't find crate for `std`
#     note: the `aarch64-apple-darwin` target may not be installed
#
# so the default release run stops at its macOS step. The binaries under
# `target/aarch64-apple-darwin/release` are from 2026-09-12 and were built before
# the pin, which is why the absence was invisible: the output was there and
# nothing rebuilt it.
#
# These two are built and never archived here - signing and notarising are Apple's
# own tools and run on a Mac - but building them on this machine is what says the
# workspace still compiles for macOS between releases.
targets = [
    "x86_64-pc-windows-msvc",
    "x86_64-unknown-linux-gnu",
    "aarch64-unknown-linux-gnu",
    "aarch64-apple-darwin",
    "x86_64-apple-darwin",
]
profile = "minimal"
