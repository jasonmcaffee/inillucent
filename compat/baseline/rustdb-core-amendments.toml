# Deliberate, reviewed changes to the retrieval engine.
#
# `rustdb-baseline verify` accepts a tracked file that differs from the
# capture only when it is named here at exactly the digest it now has, so a
# second, undeclared change to the same file still fails the guard.
#
# Written by `rustdb-baseline amend`; edit through that rather than by hand.

[[amendment]]
path = "crates/rustdb-core/src/persist.rs"
ticket = "task-1790"
reason = "phase 13 needs an index as one byte stream rather than five files, so a search generation can live inside a database and commit with the rows it describes. write_index and read_index are purely additive: no existing function, format or answer changed, and save and load still write and read the same five sections."
sha256 = "630f36edbcdcf51c9a20406359ab2f31aedff66d106f6008238af2a6f1089b1a"

