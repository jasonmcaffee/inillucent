# Every example in compat/syntax.toml, run rather than only parsed. The setup below
# is shared by every case in the file; a case's first records are its production's
# prelude. Written by `inillucent-matrix convert-syntax`.

statement ok
CREATE TABLE t(id INTEGER PRIMARY KEY, a, b, c)

statement ok
INSERT INTO t VALUES(1, 1, 2, 3), (2, 2, 3, 4), (3, NULL, 'x', x'00')

statement ok
CREATE TABLE u(id INTEGER PRIMARY KEY, a, b UNIQUE, c)

statement ok
INSERT INTO u VALUES(1, 5, 6, 7), (2, 6, 7, 8)

statement ok
CREATE TABLE a(x, y)

statement ok
INSERT INTO a VALUES(1, 2), (2, 3)

statement ok
CREATE TABLE b(x, y)

statement ok
INSERT INTO b VALUES(2, 3), (4, 5)

statement ok
CREATE TABLE c(x, y)

statement ok
INSERT INTO c VALUES(1, 1)

statement ok
CREATE VIEW v AS SELECT a, b, c FROM t

statement ok
CREATE INDEX i ON t(a)

statement ok
CREATE TRIGGER tr AFTER INSERT ON u BEGIN SELECT 1; END

case syntax-insert-stmt-p0

statement error
INSERT INTO t VALUES (1)

case syntax-insert-stmt-p1

statement ok
INSERT OR REPLACE INTO main.t AS x (a, b) VALUES (1, 2), (3, 4)

case syntax-insert-stmt-p2

statement ok
REPLACE INTO t (a) VALUES (1)

case syntax-insert-stmt-p3

statement ok
INSERT INTO t DEFAULT VALUES

case syntax-insert-stmt-p4

statement error
INSERT INTO t SELECT * FROM u

case syntax-insert-stmt-p5

statement error
WITH c AS (SELECT 1) INSERT INTO t SELECT * FROM c

case syntax-insert-stmt-p6

query I rowsort
INSERT INTO t (a) VALUES (1) RETURNING a AS x

case syntax-insert-stmt-n7

statement error
INSERT t VALUES (1)

case syntax-insert-stmt-n8

statement error
INSERT INTO t VALUES

case syntax-returning-clause-p0

query IIII rowsort
DELETE FROM t RETURNING *

case syntax-returning-clause-p1

statement error
INSERT INTO t VALUES (1) RETURNING a, b AS c

case syntax-returning-clause-p2

statement error
UPDATE t SET a = 1 RETURNING t.*

case syntax-returning-clause-n3

statement error
DELETE FROM t RETURNING

case syntax-upsert-clause-p0

statement error
INSERT INTO t VALUES (1) ON CONFLICT DO NOTHING

case syntax-upsert-clause-p1

statement error
INSERT INTO t VALUES (1) ON CONFLICT (a) DO UPDATE SET b = 1

case syntax-upsert-clause-p2

statement error
INSERT INTO t VALUES (1) ON CONFLICT (a COLLATE NOCASE) WHERE a > 0 DO UPDATE SET b = excluded.b WHERE b IS NULL

case syntax-upsert-clause-p3

statement error
INSERT INTO t VALUES (1) ON CONFLICT (a) DO NOTHING ON CONFLICT DO UPDATE SET b = 1

case syntax-upsert-clause-n4

statement error
INSERT INTO t VALUES (1) ON CONFLICT

