# Moved from crates/inillucent-compat/tests/corpora/differential-part8/misc2.cases,
# where each case was one line run through both shells with -bail. Each case now runs
# on files through the persistent oracle, and stops where the script stopped.

setup

statement ok
CREATE TABLE t(a)

statement ok
INSERT INTO t VALUES(1)

statement ok
INSERT INTO t VALUES(2)

case ms-001

query I rowsort
SELECT count(*) FROM t

setup

case ms-002

query I rowsort
SELECT 1

query I rowsort
SELECT 2

setup

statement ok
CREATE TABLE t(a)

statement ok
INSERT INTO t VALUES(1)

case ms-003

query I rowsort
SELECT a FROM t

setup

statement ok
CREATE TABLE t(a)

statement ok
INSERT INTO t SELECT 1 UNION ALL SELECT 2

case ms-004

query I rowsort
SELECT count(*) FROM t

setup

statement ok
CREATE TABLE t(a)

statement ok
INSERT INTO t VALUES(1),(2)

case ms-005

query I rowsort
UPDATE t SET a=a*10 WHERE a=1 RETURNING a

setup

statement ok
CREATE TABLE t(a,b)

statement ok
INSERT INTO t VALUES(1,2)

case ms-006

statement ok
UPDATE t SET (a,b)=(3,4)

query II rowsort
SELECT * FROM t

setup

statement ok
CREATE TABLE t(a)

statement ok
INSERT INTO t VALUES(1),(2)

case ms-007

statement ok
DELETE FROM t ORDER BY a LIMIT 1

query I rowsort
SELECT count(*) FROM t

setup

statement ok
CREATE TABLE t(a)

statement ok
INSERT INTO t VALUES(1),(2),(3)

case ms-008

statement ok
UPDATE t SET a=0 WHERE rowid IN (SELECT rowid FROM t ORDER BY a LIMIT 2)

query I rowsort
SELECT count(*) FROM t WHERE a=0

setup

statement ok
CREATE TABLE t(a)

statement ok
INSERT INTO t VALUES(1)

case ms-009

statement error
SELECT a FROM t AS x(y)

setup

case ms-010

statement error
SELECT * FROM (SELECT 1 AS a) AS x(y)

setup

statement ok
CREATE TABLE t(a)

statement ok
INSERT INTO t VALUES(1),(2)

case ms-011

query I rowsort
SELECT a FROM t WHERE a=+1

setup

case ms-012

query III rowsort
SELECT 1 AS "x y", 2 AS [z], 3 AS `q`

