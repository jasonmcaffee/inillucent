# Every example in compat/syntax.toml, run rather than only parsed. The setup below
# is shared by every case in the file; a case's first records are its production's
# prelude. Written by `inillucent-matrix convert-syntax`.

statement ok
CREATE TABLE t(id INTEGER PRIMARY KEY, a, b, c)

statement ok
INSERT INTO t VALUES(1, 1, 2, 3), (2, 2, 3, 4), (3, NULL, 'x', x'00')

statement ok
CREATE TABLE u(id INTEGER PRIMARY KEY, a, b UNIQUE, c)

statement ok
INSERT INTO u VALUES(1, 5, 6, 7), (2, 6, 7, 8)

statement ok
CREATE TABLE a(x, y)

statement ok
INSERT INTO a VALUES(1, 2), (2, 3)

statement ok
CREATE TABLE b(x, y)

statement ok
INSERT INTO b VALUES(2, 3), (4, 5)

statement ok
CREATE TABLE c(x, y)

statement ok
INSERT INTO c VALUES(1, 1)

statement ok
CREATE VIEW v AS SELECT a, b, c FROM t

statement ok
CREATE INDEX i ON t(a)

statement ok
CREATE TRIGGER tr AFTER INSERT ON u BEGIN SELECT 1; END

case syntax-create-view-stmt-p0

statement ok
DROP VIEW v

statement ok
CREATE VIEW v AS SELECT 1

case syntax-create-view-stmt-p1

statement ok
DROP VIEW v

statement error
CREATE TEMP VIEW IF NOT EXISTS main.v (x, y) AS SELECT 1, 2

case syntax-create-view-stmt-n2

statement ok
DROP VIEW v

statement error
CREATE VIEW v

case syntax-drop-view-stmt-p0

statement ok
DROP VIEW v

case syntax-drop-view-stmt-p1

statement ok
DROP VIEW IF EXISTS main.v

case syntax-drop-view-stmt-n2

statement error
DROP VIEW

