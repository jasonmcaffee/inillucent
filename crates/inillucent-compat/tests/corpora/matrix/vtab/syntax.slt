# Every example in compat/syntax.toml, run rather than only parsed. The setup below
# is shared by every case in the file; a case's first records are its production's
# prelude. Written by `inillucent-matrix convert-syntax`.

statement ok
CREATE TABLE t(id INTEGER PRIMARY KEY, a, b, c)

statement ok
INSERT INTO t VALUES(1, 1, 2, 3), (2, 2, 3, 4), (3, NULL, 'x', x'00')

statement ok
CREATE TABLE u(id INTEGER PRIMARY KEY, a, b UNIQUE, c)

statement ok
INSERT INTO u VALUES(1, 5, 6, 7), (2, 6, 7, 8)

statement ok
CREATE TABLE a(x, y)

statement ok
INSERT INTO a VALUES(1, 2), (2, 3)

statement ok
CREATE TABLE b(x, y)

statement ok
INSERT INTO b VALUES(2, 3), (4, 5)

statement ok
CREATE TABLE c(x, y)

statement ok
INSERT INTO c VALUES(1, 1)

statement ok
CREATE VIEW v AS SELECT a, b, c FROM t

statement ok
CREATE INDEX i ON t(a)

statement ok
CREATE TRIGGER tr AFTER INSERT ON u BEGIN SELECT 1; END

case syntax-create-virtual-table-stmt-p0

statement ok
DROP VIEW v

statement ok
DROP TABLE t

statement ok
CREATE VIRTUAL TABLE t USING fts5(body)

case syntax-create-virtual-table-stmt-p1

statement ok
DROP VIEW v

statement ok
DROP TABLE t

statement ok
CREATE VIRTUAL TABLE IF NOT EXISTS main.t USING rtree(id, minx, maxx)

case syntax-create-virtual-table-stmt-p2

statement ok
DROP VIEW v

statement ok
DROP TABLE t

statement error
CREATE VIRTUAL TABLE t USING mod

case syntax-create-virtual-table-stmt-n3

statement ok
DROP VIEW v

statement ok
DROP TABLE t

statement error
CREATE VIRTUAL TABLE t

