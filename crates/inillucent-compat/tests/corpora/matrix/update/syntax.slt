# Every example in compat/syntax.toml, run rather than only parsed. The setup below
# is shared by every case in the file; a case's first records are its production's
# prelude. Written by `inillucent-matrix convert-syntax`.

statement ok
CREATE TABLE t(id INTEGER PRIMARY KEY, a, b, c)

statement ok
INSERT INTO t VALUES(1, 1, 2, 3), (2, 2, 3, 4), (3, NULL, 'x', x'00')

statement ok
CREATE TABLE u(id INTEGER PRIMARY KEY, a, b UNIQUE, c)

statement ok
INSERT INTO u VALUES(1, 5, 6, 7), (2, 6, 7, 8)

statement ok
CREATE TABLE a(x, y)

statement ok
INSERT INTO a VALUES(1, 2), (2, 3)

statement ok
CREATE TABLE b(x, y)

statement ok
INSERT INTO b VALUES(2, 3), (4, 5)

statement ok
CREATE TABLE c(x, y)

statement ok
INSERT INTO c VALUES(1, 1)

statement ok
CREATE VIEW v AS SELECT a, b, c FROM t

statement ok
CREATE INDEX i ON t(a)

statement ok
CREATE TRIGGER tr AFTER INSERT ON u BEGIN SELECT 1; END

case syntax-update-stmt-p0

statement ok
UPDATE t SET a = 1

case syntax-update-stmt-p1

statement ok
UPDATE OR IGNORE main.t AS x SET a = 1, b = 2 WHERE x.a IS NULL

case syntax-update-stmt-p2

statement ok
UPDATE t SET (a, b) = (SELECT 1, 2)

case syntax-update-stmt-p3

statement ok
UPDATE t SET a = 1 FROM u WHERE t.id = u.id

case syntax-update-stmt-p4

statement ok
WITH c AS (SELECT 1) UPDATE t SET a = 1

case syntax-update-stmt-p5

query IIII rowsort
UPDATE t SET a = 1 RETURNING *

case syntax-update-stmt-n6

statement error
UPDATE t SET

case syntax-update-stmt-n7

statement error
UPDATE SET a = 1

case syntax-update-stmt-limited-p0

statement ok
UPDATE t SET a = 1 ORDER BY a LIMIT 5 OFFSET 1

