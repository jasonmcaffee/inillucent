# Every example in compat/syntax.toml, run rather than only parsed. The setup below
# is shared by every case in the file; a case's first records are its production's
# prelude. Written by `inillucent-matrix convert-syntax`.

statement ok
CREATE TABLE t(id INTEGER PRIMARY KEY, a, b, c)

statement ok
INSERT INTO t VALUES(1, 1, 2, 3), (2, 2, 3, 4), (3, NULL, 'x', x'00')

statement ok
CREATE TABLE u(id INTEGER PRIMARY KEY, a, b UNIQUE, c)

statement ok
INSERT INTO u VALUES(1, 5, 6, 7), (2, 6, 7, 8)

statement ok
CREATE TABLE a(x, y)

statement ok
INSERT INTO a VALUES(1, 2), (2, 3)

statement ok
CREATE TABLE b(x, y)

statement ok
INSERT INTO b VALUES(2, 3), (4, 5)

statement ok
CREATE TABLE c(x, y)

statement ok
INSERT INTO c VALUES(1, 1)

statement ok
CREATE VIEW v AS SELECT a, b, c FROM t

statement ok
CREATE INDEX i ON t(a)

statement ok
CREATE TRIGGER tr AFTER INSERT ON u BEGIN SELECT 1; END

case syntax-attach-stmt-p0

statement ok
ATTACH DATABASE '%SCRATCH%/other.db' AS other

case syntax-attach-stmt-p1

statement ok
ATTACH '%SCRATCH%/other.db' AS other

case syntax-attach-stmt-p2
capability attach_with_key

statement ok
ATTACH ? AS other KEY 'secret'

case syntax-attach-stmt-n3

statement error
ATTACH '%SCRATCH%/other.db'

case syntax-detach-stmt-p0

statement ok
ATTACH '%SCRATCH%/other.db' AS other

statement ok
DETACH other

case syntax-detach-stmt-p1

statement ok
ATTACH '%SCRATCH%/other.db' AS other

statement ok
DETACH DATABASE other

case syntax-detach-stmt-n2

statement ok
ATTACH '%SCRATCH%/other.db' AS other

statement error
DETACH

